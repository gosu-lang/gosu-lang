version: 2
jobs:
  build:
    environment:
      TZ: "/usr/share/zoneinfo/America/Los_Angeles"
      TERM: dumb
      MAVEN_OPTS: "-Xmx1024m"
      TEST_REPORTS_DIR: "/tmp/test-results"
    machine:
      image: circleci/classic:latest
    branches:
      ignore:
        - /rel\/.*/
    steps:
      - checkout
      - restore_cache:
          key: v1-test-{{ checksum "gosu-parent/pom.xml" }}
      - run:
          name: Run tests
          command: mvn test -B
      - run:
          name: Create test metadata folder
          command: sudo mkdir -p $TEST_REPORTS_DIR/junit/
      - run:
          name: Collect test metadata
          command: sudo find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $TEST_REPORTS_DIR/junit/ \;
      - run:
          name: Collect test stdout
          command: sudo find . -type f -regex ".*/target/surefire-reports/.*-output.txt" -exec cp {} $TEST_REPORTS_DIR/junit/ \;
      - run:
          command: ls -l $TEST_REPORTS_DIR/junit/
      - deploy:
          command: ./ci_scripts/deploy_snapshot.sh
      - store_artifacts:
          path: "$TEST_REPORTS_DIR/junit/"
      - store_test_results"
          path: "$TEST_REPORTS_DIR/junit/"
      - save_cache:
          key: v1-test-{{ checksum "gosu-parent/pom.xml" }}
          paths:
            - ~/.m2
