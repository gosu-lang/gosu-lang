package gw.util.transform.java.JavaClasses


class BasicClass  {
    var x: int = 8
    final var z: int = 2
    final var y: int

    construct(x_0: int, y_0: int) {
        this.x = x_0
        this.y = y_0 + z
    }

    property get X() : int {
        return x
    }

    property set X(x1: int) {
        x = x1
    }

}
