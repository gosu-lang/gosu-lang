package gw.spec.core.expressions.operators.precedence

class UnaryGroupPrecedenceTest extends PrecedenceTestBase {

  function testPlusIsSamePrecedenceAsMinus() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }

  function testPlusIsSamePrecedenceAsNot() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }
  
  function testPlusIsSamePrecedenceAsLogicalNot() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }
  
  function testPlusIsSamePrecedenceAsTypeof() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }
  
  function testPlusIsSamePrecedenceAsStatictypeof() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }
  
  function testMinusIsSamePrecedenceAsNot() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }
  
  function testMinusIsSamePrecedenceAsLogicalNot() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }
  
  function testMinusIsSamePrecedenceAsTypeof() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }
  
  function testMinusIsSamePrecedenceAsStatictypeof() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }
  
  function testNotIsSamePrecedenceAsLogicalNot() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }
  
  function testNotIsSamePrecedenceAsTypeof() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }
  
  function testNotIsSamePrecedenceAsStatictypeof() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }

  function testLogicalNotIsSamePrecedenceAsTypeof() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }
  
  function testLogicalNotIsSamePrecedenceAsStatictypeof() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }
  
  function testTypeofIsSamePrecedenceAsStatictypeof() {
    // Unary operators have no relative precedence, since there's no gramattical ambiguity
  }

}
