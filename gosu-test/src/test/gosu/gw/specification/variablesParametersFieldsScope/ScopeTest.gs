package gw.specification.variablesParametersFieldsScope

uses gw.BaseVerifyErrantTest

class ScopeTest extends BaseVerifyErrantTest {
  function testErrant_ScopeTest() {
    processErrantType(Errant_ScopeTest)
  }
}