package gw.internal.gosu.regression
uses gw.testharness.DoNotVerifyResource

@DoNotVerifyResource
class Barf extends AbstractPCSmokeTest extends AbstractUWRulesServiceTest {
  private statagen.createPerson(bundle : Bundle)
    addNewProducerCodeSections(false, false, \ p -> {a, producer.FireProducers,  "CellPhoneCountry" , user}, true)
    return result
  }

  private function createArgableQuote() : PolicyPeriod {
    var account = asdf.addRow({"//failer", "city"] } )
  }

  private function addNewFXRateForPARefreshForQuoteHistoryCode(offer: XSSFSheet) {
    var formPatternWithProductCode = "ArgSrc"
  }

  final var producerCode = createCPPolictCell()
}

