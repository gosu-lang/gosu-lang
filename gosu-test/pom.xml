<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <groupId>org.gosu-lang.gosu</groupId>
    <artifactId>gosu-parent</artifactId>
    <version>1.13.12</version>
    <relativePath>../gosu-parent/pom.xml</relativePath>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <artifactId>gosu-test</artifactId>

  <name>Gosu :: Test</name>

  <dependencies>
    <dependency>
      <groupId>org.gosu-lang.gosu</groupId>
      <artifactId>gosu-test-api</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.gosu-lang.gosu</groupId>
      <artifactId>gosu-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.jboss</groupId>
      <artifactId>jandex</artifactId>
      <version>2.0.0.Final</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <executions>
          <execution>
            <id>Compile Gosu test sources</id>
            <phase>test-compile</phase>
            <goals>
              <goal>testCompile</goal>
            </goals>
            <configuration>
              <compilerArgs>
                <arg>-DcheckedArithmetic=true</arg>
              </compilerArgs>
              <compilerId>gosuc</compilerId>
              <fork>true</fork>
              <compilerArgs>
                <arg>-DcheckedArithmetic=true</arg>
                <!--<arg>-Xdebug</arg>-->
                <!-- windows -->
                <!--<arg>-Xrunjdwp:transport=dt_shmem,address=gosuc,server=y,suspend=y</arg> -->
                <!-- linux/OS X -->
                <!--<arg>-Xrunjdwp:transport=dt_socket,address=5005,server=y,suspend=y</arg>-->
              </compilerArgs>
              <showWarnings>false</showWarnings>
              <useIncrementalCompilation>true</useIncrementalCompilation> <!-- this must be true or else we cannot pick up files with multiple extensions -->
              <verbose>false</verbose>
              <testIncludes>
                <include>**/*.gs</include>
                <include>**/*.gsx</include>
                <include>**/*.gst</include>
                <include>**/*.gsp</include>
              </testIncludes>
              <testExcludes>
                <exclude>**/Errant_*.gs</exclude>
                <exclude>**/Errant_*.gsx</exclude>
                <exclude>**/Errant_*.gst</exclude>
                <exclude>**/Errant_*.gsp</exclude>
              </testExcludes>
            </configuration>
          </execution>
        </executions>
        <dependencies>
          <dependency>
            <groupId>org.gosu-lang.gosu</groupId>
            <artifactId>gosu-maven-compiler</artifactId>
            <version>${project.version}</version>
          </dependency>
        </dependencies>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>-Dgw.tests.skip.knownbreak=true -DcheckedArithmetic=true -Xmx4g</argLine>
          <includes>
            <include>gw/config*/**/*Test.class</include>
            <include>gw/internal*/**/*Test.class</include>
            <include>gw/lang*/**/*Test.class</include>
            <include>gw/spec*/**/*Test.class</include>
            <include>gw/test*/**/*Test.class</include>
            <include>gw/util*/**/*Test.class</include>
            <include>gw/xml*/**/*Test.class</include>
          </includes>
          <excludes>
            <exclude>gw/internal/gosu/parser/java/*</exclude>
            <exclude>gw/test/SuiteTest*</exclude>
            <exclude>**/*BenchmarkTest*</exclude>
            <exclude>**/junit/framework/TestSuite*BenchmarkTest*</exclude>
          </excludes>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>add-test-source</id>
            <phase>generate-test-sources</phase>
            <goals>
              <goal>add-test-source</goal>
            </goals>
            <configuration>
              <sources>
                <source>src/test/gosu</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  
  <reporting>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-report-plugin</artifactId>
        <version>2.19.1</version>
      </plugin>
    </plugins>
  </reporting>
  
</project>
